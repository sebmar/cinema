<%= form_for([@movie,@showing]) do |f| %>
  <% if @showing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@showing.errors.count, "error") %> prohibited this showing from being saved:</h2>

      <ul>
      <% @showing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :screening %><br>
    <%= f.datetime_select :screening %>
  </div>
  <div class="field">
    <%= f.collection_select(:screen_id, Screen.all, :id, :name, { :label => false, :selected => @showing.screen_id }, { class: 'form-control' }) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
